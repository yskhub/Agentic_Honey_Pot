# Example environment variables for development and deployment
# Copy this to .env and fill sensitive values before running.

# API keys
API_KEY=your_api_key_here
ADMIN_API_KEY=your_admin_key_here

# Outgoing delivery endpoint (optional for testing)
OUTGOING_ENDPOINT=

# Database
DATABASE_URL=sqlite:///data/sentinel.db

# Server
DEV_SERVER_PORT=8030

# GUVI callback key (if used)
GUVI_API_KEY=

# Detector threshold (0.0-1.0)
DETECTOR_THRESHOLD=0.5

# Admin auth options (choose one of the options below)

# 1) Static PEM public key (optional)
# Paste your PEM on a single line or load from a secrets manager in production.
ADMIN_AUTH_PUBLIC_KEY=
ADMIN_AUTH_JWT_ALGS=RS256

# 2) JWKS / OIDC discovery (recommended for federated identity)
ADMIN_JWKS_URL=
ADMIN_OIDC_ISSUER=
ADMIN_JWT_AUDIENCE=
ADMIN_JWT_ISSUER=
ADMIN_JWKS_CACHE_TTL=300

# Additional runtime options
# OUTGOING_API_KEY can be used by the outgoing worker when calling external receivers.
OUTGOING_API_KEY=

# Note: Do NOT commit real secrets to source control. Use a secret store or CI secrets.
